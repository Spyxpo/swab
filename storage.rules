rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return request.auth.uid == userId;
    }

    function isValidImage() {
      return request.resource.contentType.matches('image/.*') &&
             request.resource.size < 10 * 1024 * 1024; // 10MB max
    }

    function isValidKeystore() {
      return request.resource.size < 20 * 1024 * 1024; // 20MB max
    }

    function isValidCertificate() {
      return request.resource.size < 20 * 1024 * 1024; // 20MB max
    }

    function isValidProvisioningProfile() {
      return request.resource.size < 10 * 1024 * 1024; // 10MB max
    }

    // User's personal storage folder
    // Each user has their own folder: /users/{userId}/
    match /users/{userId}/{allPaths=**} {
      // Allow read if user owns the folder
      allow read: if isSignedIn() && isOwner(userId);

      // Allow write if user owns the folder
      allow write: if isSignedIn() && isOwner(userId);
    }

    // Project icons
    // Path: /projects/{userId}/icons/{filename}
    match /projects/{userId}/icons/{filename} {
      allow read: if isSignedIn() && isOwner(userId);
      allow write: if isSignedIn() && isOwner(userId) && isValidImage();
      allow delete: if isSignedIn() && isOwner(userId);
    }

    // Project assets folder
    // Path: /projects/{userId}/assets/{filename}
    match /projects/{userId}/assets/{filename} {
      allow read: if isSignedIn() && isOwner(userId);
      allow write: if isSignedIn() && isOwner(userId);
      allow delete: if isSignedIn() && isOwner(userId);
    }

    // Android keystores
    // Path: /projects/{userId}/keystores/{filename}
    match /projects/{userId}/keystores/{filename} {
      allow read: if isSignedIn() && isOwner(userId);
      allow write: if isSignedIn() && isOwner(userId) && isValidKeystore();
      allow delete: if isSignedIn() && isOwner(userId);
    }

    // Apple certificates
    // Path: /projects/{userId}/certificates/{filename}
    match /projects/{userId}/certificates/{filename} {
      allow read: if isSignedIn() && isOwner(userId);
      allow write: if isSignedIn() && isOwner(userId) && isValidCertificate();
      allow delete: if isSignedIn() && isOwner(userId);
    }

    // Apple provisioning profiles
    // Path: /projects/{userId}/profiles/{filename}
    match /projects/{userId}/profiles/{filename} {
      allow read: if isSignedIn() && isOwner(userId);
      allow write: if isSignedIn() && isOwner(userId) && isValidProvisioningProfile();
      allow delete: if isSignedIn() && isOwner(userId);
    }

    // Build outputs (temporary storage)
    // Path: /builds/{userId}/{buildId}/{filename}
    match /builds/{userId}/{buildId}/{filename} {
      allow read: if isSignedIn() && isOwner(userId);
      allow write: if isSignedIn() && isOwner(userId);
      allow delete: if isSignedIn() && isOwner(userId);
    }

    // Deny all other access by default
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
